{
  "jobDefinitionName": "${name}",
  "type": "container",
  "containerProperties": {
    "image": "${image_uri}",
    "memory": ${memory},
    "vcpus": ${memory},
    "jobRoleArn": "${job_role_arn}",
    "environment": ${environment_vars}
  }
}